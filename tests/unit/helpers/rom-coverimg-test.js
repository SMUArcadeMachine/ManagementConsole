import { romCoverimg } from 'managementdashboard/helpers/rom-coverimg';
import { module, test } from 'qunit';

module('Unit | Helper | rom coverimg');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = romCoverimg([42]);
  assert.ok(result);
});
