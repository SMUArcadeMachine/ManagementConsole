import { toggleDeactivate } from 'managementdashboard/helpers/toggle-deactivate';
import { module, test } from 'qunit';

module('Unit | Helper | toggle deactivate');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toggleDeactivate([42]);
  assert.ok(result);
});
